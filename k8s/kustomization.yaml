apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - k8s.yaml
  - oauth2-proxy.yaml
  - oauth2-proxy-sealedsecret.yaml
  - ingress.yaml
  # argocd.yaml excluded — contains AppProject/Application managed from infra repo
